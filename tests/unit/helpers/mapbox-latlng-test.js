import { mapboxLatlng } from '../../../helpers/mapbox-latlng';
import { module, test } from 'qunit';

module('Unit | Helper | mapbox latlng');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = mapboxLatlng(42);
  assert.ok(result);
});
