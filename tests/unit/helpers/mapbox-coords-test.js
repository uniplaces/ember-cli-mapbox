import { mapboxCoords } from '../../../helpers/mapbox-coords';
import { module, test } from 'qunit';

module('Unit | Helper | mapbox coords');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = mapboxCoords(42);
  assert.ok(result);
});
